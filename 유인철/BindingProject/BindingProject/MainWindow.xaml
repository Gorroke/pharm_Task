<Window x:Class="BindingProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BindingProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--
    <Window.DataContext>
        <local:TestViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBox Width="200" Height="30" Text="{Binding Path=TestText, Mode=TwoWay}"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBlock Width="200" Height="30" Text="{Binding TestText}"/>
        </Grid>
        <Grid Grid.Row="2">
            <TextBox Width="200" Height="30"/>
        </Grid>
    </Grid>
    -->
    <Grid>
        <!--<local:TestView/> TextBox 입력 시 Label 값 변경 / 버튼 클릭시 ICommand로-->
        <local:TestViewTwo/> <!-- TextBox에 색 입력하면 밑 패널 색상 변경-->
    </Grid>
</Window>
